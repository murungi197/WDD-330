This branch adds the dynamic product list feature (ProductList, utils.renderListWithTemplate, main.js) and moves json/images into `src/public` so Vite copies them.

This file was added to create a visible difference for a PR comparison.
